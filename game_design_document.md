# 坦克大战游戏架构设计文档

## 1. 引言

本文档旨在详细阐述即将开发的“坦克大战”游戏的软件架构。该游戏基于用户提出的需求，将采用 Python 及其 Pygame 库进行开发，目标是实现一款支持本地多人对战、拥有智能AI敌人、多关卡、特殊道具、计分系统，并采用2D像素艺术风格的桌面游戏。本文档将作为后续开发工作的指导蓝图，确保各模块协调一致，功能完整实现。

## 2. 核心设计理念

游戏的核心设计将围绕模块化和可扩展性展开。我们将采用面向对象的设计方法，将游戏中的各个实体（如坦克、子弹、道具）和系统（如碰撞检测、AI、关卡管理）抽象为独立的类和模块。这种设计有助于降低模块间的耦合度，方便独立开发、测试和后续的功能迭代。我们将优先保证核心玩法的稳定性和趣味性，再逐步完善其他辅助功能和视觉音效表现。

## 3. 系统架构概述

游戏整体架构将分为几个主要层面：表现层（处理用户输入、图形渲染和音效播放）、逻辑层（处理游戏规则、实体行为、AI决策、碰撞检测等）和数据层（管理游戏资源、关卡数据、玩家状态等）。

### 3.1 游戏主控模块 (GameManager)

这是游戏的总指挥中心，负责初始化 Pygame 环境、管理游戏的主循环、处理全局事件（如退出游戏）、控制游戏状态的切换（例如，从主菜单到游戏进行中，再到游戏结束界面）。它将协调其他所有模块的运作，确保游戏流程的顺畅进行。该模块还将负责加载和管理全局配置，如屏幕分辨率、音量等。

### 3.2 实体模块 (Entities)

游戏中的动态对象将被抽象为实体类。主要包括：

*   **坦克 (Tank)**：作为所有坦克的基类，封装坦克共有的属性（如位置、方向、速度、生命值、所属阵营）和行为（如移动、旋转、开火、受到伤害）。它将包含更新自身状态的逻辑，并处理与地图边界的交互。
*   **玩家坦克 (PlayerTank)**：继承自 `Tank` 类，专门处理玩家的输入控制。将支持至少两名玩家，每名玩家使用不同的按键组合控制各自的坦克进行移动和射击。玩家坦克还将记录得分等特定于玩家的数据。
*   **敌人坦克 (EnemyTank)**：同样继承自 `Tank` 类，但其行为由AI模块驱动。敌人坦克将具备自主移动、索敌、射击以及可能的规避行为。我们将设计不同类型的敌人坦克，它们可能在移动速度、火力、AI智能程度上有所区别，以增加游戏的挑战性和多样性。
*   **子弹 (Bullet)**：代表坦克发射的炮弹。子弹类将包含其发射者信息、飞行速度、方向、伤害值等属性。子弹在发射后会沿直线飞行，直到击中目标或飞出屏幕边界后销毁。不同类型的坦克或道具可能会产生不同效果的子弹。
*   **道具 (PowerUp)**：代表游戏中可拾取的特殊物品。道具类将包含其类型（如增强火力、提升移速、增加生命、临时护盾等）、位置以及持续时间（如果适用）。道具会在地图上随机或固定位置生成，玩家坦克接触后触发相应效果。

### 3.3 地图与关卡管理模块 (MapManager & LevelManager)

*   **地图 (Map)**：负责加载和渲染游戏地图。地图将采用基于图块（Tile-based）的设计，每个图块代表一种地形（如空地、砖墙、钢墙、草地、水域等）。地图模块将提供查询特定位置地形类型的功能，并处理坦克与不可通行地形的碰撞。砖墙等可破坏地形在被子弹击中后会改变状态或消失。
*   **关卡管理 (LevelManager)**：负责管理游戏的多个关卡。它将定义每个关卡的地图布局、敌人坦克的类型、数量和出生点、道具的生成规则等。当满足特定条件（如消灭当前关卡所有敌人）后，关卡管理器将加载并切换到下一个关卡。它也负责处理游戏胜利（通关所有关卡）和游戏失败（玩家生命耗尽或基地被毁）的逻辑。

### 3.4 碰撞检测模块 (CollisionDetector)

这是一个关键模块，负责实时检测游戏中各个实体之间的碰撞。主要检测以下几种碰撞类型：

*   子弹与坦克之间的碰撞（区分敌我，计算伤害）。
*   子弹与地图障碍物（如墙壁）的碰撞（子弹销毁，墙壁可能损坏）。
*   坦克与地图障碍物的碰撞（阻止坦克移动）。
*   坦克与坦克之间的碰撞（阻止移动，或根据游戏规则产生其他效果）。
*   坦克与道具的碰撞（触发道具效果）。

碰撞检测的效率和准确性对游戏体验至关重要，我们将采用合适的算法（如矩形重叠检测）来实现。

### 3.5 人工智能模块 (AIController)

该模块专为敌人坦克设计，赋予它们一定的智能行为。AI将包括以下几个方面：

*   **感知系统**：使敌人坦克能够“感知”到周围环境，如玩家坦克的位置、障碍物分布等。
*   **决策系统**：根据感知到的信息和预设的AI逻辑（如攻击型、防御型、巡逻型），决定下一步的行动（移动、转向、射击）。
*   **路径规划**：使敌人坦克能够找到通往目标（如玩家坦克）的路径，并能避开障碍物。可能会采用简单的寻路算法，如基于方向的试探或更复杂的A*算法的简化版本，以适应2D像素风格游戏的性能要求。
*   **射击逻辑**：控制敌人坦克的开火时机和目标选择，例如在瞄准玩家时开火，或在一定冷却时间后自动开火。

我们将努力使AI行为看起来自然且具有挑战性，避免过于简单或可预测的模式。

### 3.6 用户界面模块 (UIManager)

负责游戏界面的显示和交互。包括：

*   **游戏主界面 (HUD)**：实时显示玩家的得分、剩余生命、当前关卡、敌人数量、已激活的道具效果及其剩余时间等重要信息。
*   **菜单系统**：包括主菜单（开始游戏、设置选项、退出游戏）、暂停菜单（继续游戏、重新开始、返回主菜单）和游戏结束界面（显示最终得分、通关祝贺或失败提示，并提供重新开始或返回主菜单的选项）。

界面元素将采用与游戏整体2D像素风格一致的设计。

### 3.7 资源管理模块 (AssetManager)

负责加载、管理和提供游戏所需的各种资源，包括：

*   **图像资源**：坦克、子弹、爆炸效果、地图图块、道具图标、UI元素等的2D像素图片。
*   **音效资源**：坦克移动、射击、爆炸、道具拾取、背景音乐等。

该模块将确保资源在需要时能够被高效加载，并进行适当的缓存以优化性能。

### 3.8 输入处理模块 (InputHandler)

负责捕获和处理来自用户的键盘输入。它会将原始的按键事件转换为游戏内的具体指令，如玩家坦克的移动、转向、射击，以及菜单操作等。支持双人对战的不同按键映射。

### 3.9 计分系统 (ScoreManager)

负责跟踪和更新玩家的得分。当玩家成功击毁敌人坦克或完成特定目标时，计分系统会相应增加其分数。得分信息将通过UI模块显示给玩家。

## 4. 核心机制详述

### 4.1 多人对战

游戏将支持本地双人对战模式。两名玩家将共享同一屏幕，使用不同的键盘控制各自的坦克。游戏逻辑需要正确处理两个玩家的独立输入、状态更新和得分计算。未来可考虑扩展至网络对战，但这超出了当前版本的范围。

### 4.2 关卡与地图设计

关卡数据将以外部文件（如JSON或自定义文本格式）的形式存储，方便编辑和扩展。每个关卡文件将定义地图布局（砖墙、钢墙、草地、水域等元素的位置）、敌人的初始位置和类型、道具的可能出现位置等。地图将是基于网格的，便于碰撞检测和AI寻路。

### 4.3 道具系统

道具将在地图上随机或预设位置生成。玩家坦克接触到道具后，会触发相应的增益效果，如临时无敌、增加射速、强化子弹威力、冻结敌人等。道具效果通常有持续时间限制。

### 4.4 2D像素艺术风格

所有游戏内视觉元素，包括坦克、子弹、爆炸、地图元素、UI等，都将采用统一的2D像素艺术风格。这将通过精心设计的 spritesheet 和图块集来实现。

## 5. GitHub集成

开发过程中的代码将通过Git进行版本控制，并最终推送到用户提供的GitHub仓库 (https://github.com/bty418/tanktest.git)。我们将使用用户提供的Personal Access Token进行认证推送。项目结构将保持清晰，便于他人理解和协作。

## 6. 总结

本架构设计文档为“坦克大战”游戏的开发提供了全面的规划。通过模块化的设计和对核心机制的详细定义，我们期望能够构建一个功能丰富、稳定可靠且具有良好用户体验的游戏。在后续的开发过程中，我们将严格遵循此设计，并根据实际情况进行必要的调整和优化。
